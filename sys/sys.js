
// PRAGMA CORE :: REGISTER
// ========================================================================================================

   // strict mode :: standard
   // -----------------------------------------------------------------
      "use strict";
   // -----------------------------------------------------------------

   // set cwd :: required
   // -----------------------------------------------------------------
      process.chdir(__dirname);
   // -----------------------------------------------------------------

   // log service :: log stdout & stderr to server/log
   // -----------------------------------------------------------------
      require('./stdlog.js');
   // -----------------------------------------------------------------

   // terminaton :: for process shutdown
   // -----------------------------------------------------------------
      require('./shutdown.js');
   // -----------------------------------------------------------------

   // core config :: system configuration
   // -----------------------------------------------------------------
      var conf = require('../cfg/core.json');
   // -----------------------------------------------------------------

// ========================================================================================================


// PRAGMA CORE :: INITIATE
// ========================================================================================================

   // start hardware monitor
   // -----------------------------------------------------------------
      hwmon.init();
   // -----------------------------------------------------------------

   // assign services to mules according to config
   // -----------------------------------------------------------------
      conf.init.forEach(function(itm, key, arr)
      {
         mule.assign(itm.keys[0], itm[itm.keys[0]].mules);
      });
   // -----------------------------------------------------------------

// ========================================================================================================
