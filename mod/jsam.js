
// JSAM INTEGRATION
// ========================================================================================================

   // strict mode :: standard
   // -----------------------------------------------------------------
      "use strict";
   // -----------------------------------------------------------------


   // node vm :: required
   // -----------------------------------------------------------------
      var vmNode = require('vm');
   // -----------------------------------------------------------------


   // global object :: register
   // -----------------------------------------------------------------
      global.JSAM = {};
   // -----------------------------------------------------------------


   // compilers :: register
   // -----------------------------------------------------------------
      JSAM.compiler = (function()
      {
         var rsl = {};

         path.readdirSync('./ext/jsam').forEach(function(v,k,a)
         {
            rsl[k] = require('./ext/jsam/'+v);
         });

         return rsl;
      }());
   // -----------------------------------------------------------------

// ========================================================================================================


// JSAM :: PARSE
// ========================================================================================================
   JSAM.parse = function(str, arg)
   {

   };
// ========================================================================================================
